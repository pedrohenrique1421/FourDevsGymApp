export function HandleNext(matricula, dtNasc) {
    // FUNCAO QUEBRADA PARA FINS DE DESENVOLVIMENTO
    if (!matricula.length > 0 && !dtNasc.length > 0) {
        return true;
    } else {
        return false;
    }
}
